"""Branch C gate experiment: does a CAUSALLY-FILTERED per-match latent
"conditions" factor improve T1 per-ball outcome prediction (K=11), t20 only,
beyond frozen B3?

Non-negotiable formulation: theta_hat(m, t) may be estimated ONLY from balls
strictly before t in the SAME match. Reuses the frozen M2 harness.
"""

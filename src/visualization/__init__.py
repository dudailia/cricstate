"""Publication figure + table generation for the negative-result paper.

Reads results/summary.json (the frozen evidence set) and renders static PNGs
and markdown tables. Runs no models and touches no experiment harness.
"""
